{% extends "::base.html.twig" %}

{% block title %}Umfrage erstellen{% endblock %}

{% block body %}
    {% include'AppBundle:Templates:header.html.twig' %}

    <div class="add-changeNavigation">
        <a class="add-changeNav" href="{{ path('addSurvey') }}">Umfrage erstellen</a>
        <a class="add-changeNav active" href="{{ path('addDevice') }}">Geräte erstellen</a>
        <a class="add-changeNav" href="{{ path('addUser') }}">Nutzer erstellen</a>
    </div>

    <div class="container row">
        <div class="add-addition add col s6 push-s3">
            <div class="wrapper row">
                <div class="add-devices add col s12">
                    <form action="{{ path('addDevice') }}" method="post">
                        <input name="newdevice" placeholder="neues Gerät">
                        <button class="newInsert-button">Erstellen</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}